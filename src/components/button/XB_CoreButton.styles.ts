import {StyleSheet} from 'react-native';

export const Styles = StyleSheet.create({
  buttonContainer: {
    flex: 1,
  },
  submit: {
    borderRadius: 100,
    alignItem: 'center',
    justifyContent: 'center',
  },
  buttonBody: {
    padding: 5,
    borderRadius: 100,
    margin: 5,
    alignItems: 'center',
  },
  text: {
    fontSize: 16,
    lineHeight: 26,
    textAlign: 'center',
  },
});
